## 🚀 GPU Support

This project supports NVIDIA GPU acceleration via Ollama.

Requirements:
- NVIDIA GPU
- NVIDIA drivers installed
- nvidia-container-toolkit (for Docker)

If not available, the system will fallback to CPU.